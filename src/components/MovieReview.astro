---
interface CategoryScore {
  name: string;
  score: number;
}

interface Props {
  score: number;
  reviewText: string;
  categories?: CategoryScore[];
}

const { score, reviewText, categories = [] } = Astro.props;
---

<section class="movie-review">
  <p class="review-heading">Opinión SmallParadiso</p>

  <div class="review-score" aria-label={`Puntuación ${score} de 10`}>
    <span class="score-number">{score.toFixed(1)}</span>
    <span class="score-max">/ 10</span>
  </div>

  <div class="review-body">
    <p>{reviewText}</p>
  </div>

  {categories.length > 0 && (
    <div class="category-scores" role="list" aria-label="Puntuaciones por categoria">
      {categories.map((cat) => (
        <div class="category-row" role="listitem">
          <span class="category-name">{cat.name}</span>
          <div class="category-bar-track">
            <div
              class="category-bar-fill"
              style={`width: ${cat.score * 10}%`}
              aria-hidden="true"
            ></div>
          </div>
          <span class="category-value">{cat.score}</span>
        </div>
      ))}
    </div>
  )}
</section>

<style>
  .movie-review {
    padding: var(--space-6) 0;
    border-bottom: 1px solid var(--color-border);
    margin-bottom: var(--space-6);
  }

  .review-heading {
    font-size: .82rem;
    font-weight: 800;
    letter-spacing: .04em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-4);
  }

  .review-score {
    display: flex;
    align-items: baseline;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
  }

  .score-number {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 3rem;
    letter-spacing: -0.01em;
    line-height: 1;
    color: var(--color-accent);
    font-variant-numeric: tabular-nums;
  }

  .score-max {
    font-size: .9rem;
    color: var(--color-text-tertiary);
  }

  .review-body {
    max-width: 58ch;
    margin-bottom: var(--space-6);
  }

  .review-body p {
    font-size: var(--text-base);
    line-height: 1.7;
    color: var(--color-text-secondary);
  }

  .category-scores {
    display: grid;
    gap: var(--space-3);
    max-width: 400px;
  }

  .category-row {
    display: grid;
    grid-template-columns: 100px 1fr 32px;
    align-items: center;
    gap: var(--space-3);
  }

  .category-name {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
  }

  .category-bar-track {
    height: 4px;
    background-color: var(--color-border);
    border-radius: var(--radius-full);
    overflow: hidden;
  }

  .category-bar-fill {
    height: 100%;
    background-color: var(--color-accent);
    border-radius: var(--radius-full);
    transition: width var(--transition-slow);
  }

  .category-value {
    font-size: var(--text-sm);
    font-weight: 600;
    font-variant-numeric: tabular-nums;
    text-align: right;
    color: var(--color-text);
  }

  @media (max-width: 640px) {
    .category-row {
      grid-template-columns: 80px 1fr 28px;
    }
  }
</style>
